{
  "$schema": "https://caplets.dev/config.schema.json",
  "mcpServers": {
    "browser-use": {
      "name": "Browser Use",
      "description": "Web browsing capabilities to fetch real-time information from the internet, including web pages, APIs, and other online resources.",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--extension",
        "--browser=chromium",
        "--executable-path=$env:DEFAULT_BROWSER_EXECECUTABLE_PATH"
      ],
      "env": {
        "PLAYWRIGHT_MCP_EXTENSION_TOKEN": "$env:PLAYWRIGHT_MCP_EXTENSION_TOKEN",
        "PWTEST_EXTENSION_USER_DATA_DIR": "$env:DEFAULT_BROWSER_USER_DATA_DIR"
      },
      "shadowing": "allow"
    },
    "computer-use": {
      "name": "Computer Use",
      "description": "Computer use capabilities to control your computer, including opening and closing applications, opening and closing windows, and more.",
      "command": "npx",
      "args": ["-y", "open-computer-use@latest", "mcp"],
      "shadowing": "allow"
    },
    "deepwiki": {
      "name": "DeepWiki",
      "description": "DeepWiki provides up-to-date documentation you can talk to, for every repo in the world. Think Deep Research for GitHub - powered by Devin.",
      "url": "https://mcp.deepwiki.com/mcp"
    },
    "github": {
      "name": "GitHub",
      "description": "Inspect and manage GitHub repositories, issues, pull requests, branches, commits, and code review workflows.",
      "url": "https://api.githubcopilot.com/mcp",
      "auth": {
        "type": "bearer",
        "token": "$vault:GH_TOKEN"
      }
    },
    "stealth-browser-use": {
      "name": "Stealth Browser Use",
      "description": "A stealth web browser that can evade anti-bot measures and access content that may be restricted to regular automated browsers.",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--config=$env:HOME/.config/caplets/stealth-playwright.json",
        "--browser=firefox",
        "--executable-path=$env:CAMOUFOX_EXECECUTABLE_PATH",
        "--user-data-dir=$env:CAMOUFOX_USER_DATA_DIR",
        "--headless"
      ],
      "shadowing": "allow"
    }
  },
  "googleDiscoveryApis": {
    "gmail": {
      "name": "Gmail",
      "description": "Access and manage your email inbox, including sending, reading, and deleting emails.",
      "discoveryUrl": "https://gmail.googleapis.com/$discovery/rest?version=v1",
      "auth": {
        "type": "oauth2",
        "issuer": "https://accounts.google.com",
        "clientId": "$vault:GOOGLE_CLIENT_ID",
        "clientSecret": "$vault:GOOGLE_CLIENT_SECRET"
      }
    },
    "google-drive": {
      "name": "Google Drive",
      "description": "Access and manage your Google Drive files, including uploading, downloading, and deleting files.",
      "discoveryUrl": "https://www.googleapis.com/discovery/v1/apis/drive/v3/rest",
      "auth": {
        "type": "oauth2",
        "issuer": "https://accounts.google.com",
        "clientId": "$vault:GOOGLE_CLIENT_ID",
        "clientSecret": "$vault:GOOGLE_CLIENT_SECRET"
      }
    },
    "google-tasks": {
      "name": "Google Tasks",
      "description": "Access and manage your Google Tasks, including creating, updating, and deleting tasks.",
      "discoveryUrl": "https://www.googleapis.com/discovery/v1/apis/tasks/v1/rest",
      "auth": {
        "type": "oauth2",
        "issuer": "https://accounts.google.com",
        "clientId": "$vault:GOOGLE_CLIENT_ID",
        "clientSecret": "$vault:GOOGLE_CLIENT_SECRET"
      }
    }
  }
}
