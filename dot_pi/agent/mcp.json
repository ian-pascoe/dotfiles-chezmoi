{
  "settings": {
    "showStatusIcon": false
  },
  "mcpServers": {
    "executor": {
      "url": "${EXECUTOR_BASE_URL}/mcp?elicitation_mode=native",
      "headers": {
        "Authorization": "Bearer ${EXECUTOR_API_KEY}"
      },
      "lifecycle": "keep-alive",
      "directTools": true
    }
  }
}
